{
  "name": "@bladets/tempo",
  "version": "0.3.0",
  "description": "Blade template integration for @tempots/dom - reactive rendering",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "homepage": "https://github.com/fponticelli/blade#readme",
  "bugs": {
    "url": "https://github.com/fponticelli/blade/issues"
  },
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext ts --report-unused-disable-directives --max-warnings 0",
    "lint:fix": "eslint . --ext ts --fix",
    "typecheck": "tsc --noEmit",
    "check": "pnpm run lint:fix && pnpm run lint && pnpm run build && pnpm run test",
    "clean": "rm -rf dist coverage .turbo",
    "prepublishOnly": "pnpm run check"
  },
  "keywords": [
    "blade",
    "template",
    "tempo",
    "tempots",
    "reactive",
    "rendering"
  ],
  "author": "Franco Ponticelli",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fponticelli/blade.git",
    "directory": "packages/blade-tempo"
  },
  "peerDependencies": {
    "@bladets/template": "^0.4.0",
    "@tempots/dom": "^37.0.0"
  },
  "devDependencies": {
    "@bladets/template": "workspace:*",
    "@tempots/dom": "^37.0.3",
    "@types/node": "^22.19.11",
    "@vitest/coverage-v8": "^4.0.18",
    "@vitest/ui": "^4.0.18",
    "jsdom": "^28.1.0",
    "vite": "^7.3.1",
    "vite-plugin-dts": "^4.5.4",
    "vitest": "^4.0.18"
  }
}
